{% extends 'base.html.twig' %}

{% block title %}Activite{% endblock %}

{% block body %}
    <h1>Activite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ activite.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ activite.description }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ activite.code }}</td>
            </tr>
            <tr>
                <th>DebutActivite</th>
                <td>{{ activite.debutActivite }}</td>
            </tr>
            <tr>
                <th>FinActivite</th>
                <td>{{ activite.finActivite }}</td>
            </tr>
            <tr>
                <th>DebutPeriode</th>
                <td>{{ activite.debutPeriode }}</td>
            </tr>
            <tr>
                <th>FinPeriode</th>
                <td>{{ activite.finPeriode }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ activite.slug }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ activite.createdAt ? activite.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('conafor_activite_index') }}">back to list</a>

    <a href="{{ path('conafor_activite_edit', {'id': activite.id}) }}">edit</a>

    {{ include('conafor_activite/_delete_form.html.twig') }}
{% endblock %}
